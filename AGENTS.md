# Repository Guidelines

## Commit Messages

All commit messages must use the following format:

```text
name(scope): message
```

Keep `name` and `scope` concise, and write `message` as a clear summary of the change.

Examples:

```text
fix(tts): update Papago API integration
test(api): add TTS response coverage
docs(repo): add contributor instructions
```
